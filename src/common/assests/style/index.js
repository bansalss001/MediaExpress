/*
    @author SushantBansal
    @email bansalss001@gmail.com
*/

import { AppColor } from './lib/appColors';
import { CalculateFontSize, CalculateLineHeight, AddCommasToNumber, FontForWeight } from './lib/styleFunctions';
import { StyleBuilder } from './lib/styleBuilder';
import StyleClass from './styleClass/style';

/*
    @AppColor contains all the Colors HexCode used in App
    @CalculateFontSize -> Calculate Font Size as per DPI
    @CalculateLineHeight -> Calculate Line Hieght as per DPI
    @AddCommasToNumber -> contains all the StyleFuntions For Better Display of App
*/

export {
    AppColor,
    CalculateFontSize,
    CalculateLineHeight,
    AddCommasToNumber,
    FontForWeight,
    StyleBuilder,
    StyleClass
}

