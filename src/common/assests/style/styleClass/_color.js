import { AppColor} from '../index';
import { Color } from '../../constants';

export default ColorStyle = {
    backgroundWhite : {
        backgroundColor : Color.White
    },
    textWhite : {
        color : Color.White
    }
}
